language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-python-package' ]; then cd .. && eval "mv $currentfolder generator-python-package" && cd generator-python-package; fi

